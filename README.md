# Music-Playlist
This project is a music playlist manager implemented using a doubly linked list. Each song is a node with next and previous pointers. It supports operations like add, delete, search, play, shuffle, and repeat. The current pointer helps in navigation, making it efficient for forward and backward traversal.”
